// import { RESULTS } from '../actions';

const INITAL_STATE = [];

const results = (state = INITAL_STATE, action) => {
  switch (action.type) {
  // case RESULTS:
  //   return action.value;
  default:
    return state;
  }
};

export default results;
